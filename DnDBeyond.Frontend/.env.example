# API Configuration
# The frontend uses Vite's proxy configuration to connect to the backend
# See vite.config.ts for proxy settings

# Backend API URL (default: http://localhost:5000)
# VITE_API_URL=http://localhost:5000
